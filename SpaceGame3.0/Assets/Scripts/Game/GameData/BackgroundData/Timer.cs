using UnityEngine;
using System.Collections;

public class Timer : MonoBehaviour 
{
    public void RunTimer(GameObject obj, float time)
    {
        time -= Time.deltaTime;

        if(time == 0 )
        {
            
        }
    }
}
