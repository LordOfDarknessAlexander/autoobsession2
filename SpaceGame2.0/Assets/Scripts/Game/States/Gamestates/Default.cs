using UnityEngine;
using System.Collections;

public class Default : MonoBehaviour 
{
    /*private float gameTimer_;

    public Paused mPausedState;

    public override void OnStateEntered()
    {
        gameTimer_ = 0.0f;
    }

    public override void OnStateExit()
    {
        Debug.Log("Exiting default game state. Default state ran for " + 
                    gameTimer_ + " seconds");
    }

    public override void StateUpdate()
    {
        gameTimer_ += Time.deltaTime;

        if (Input.GetAxis("Pause") > 0.0f)
        {
            //mStateMachine.ChangeState(mPausedState);
        }
    }

    public override void StateGUI()
    {
       // GUI.Label(new Rect(10.0f, 10.0f, 200.0f, 50.0f), gameTimer_.ToString());
    }*/
}
